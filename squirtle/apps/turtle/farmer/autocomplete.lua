-- local completion = require "cc.shell.completion"
-- local complete = completion.build(nil, {completion.choice, {"forward", "up", "down"}})
-- shell.setCompletionFunction("squirtle/apps/farmer/app.lua", complete)
shell.setAlias("farmer", "squirtle/apps/turtle/farmer/app.lua")
