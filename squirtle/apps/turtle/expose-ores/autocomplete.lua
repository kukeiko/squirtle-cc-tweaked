-- local completion = require "cc.shell.completion"
-- local complete = completion.build(nil, {completion.choice, {"forward", "up", "down"}})
-- shell.setCompletionFunction("squirtle/apps/expose-ores/app.lua", complete)
shell.setAlias("expose-ores", "squirtle/apps/turtle/expose-ores/app.lua")
